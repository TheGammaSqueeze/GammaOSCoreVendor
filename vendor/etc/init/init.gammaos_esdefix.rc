service esde_fix /vendor/bin/esde_fix.sh
    class main
    user root
    group root system
    oneshot
    disabled

on property:sys.boot_completed=1 && property:sys.user.0.ce_available=true && property:getprop persist.sys.device_provisioned=1
   start esde_fix
